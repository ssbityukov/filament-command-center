@props(['output'])

<pre class="max-h-[32rem] overflow-auto rounded-lg bg-gray-950 p-4 font-mono text-xs leading-relaxed text-gray-100 dark:bg-black">{{ $output === '' ? 'No output.' : $output }}</pre>
